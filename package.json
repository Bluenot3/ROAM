{
  "name": "roam-backend",
  "type": "module",
  "version": "1.0.0",
  "description": "ROAM — Document any website, automatically",
  "scripts": {
    "start": "node backend.mjs",
    "build": "cd shot-app && npm install && npm run build",
    "install:all": "npm install && cd shot-app && npm install",
    "dev": "node backend.mjs",
    "postinstall": "node -e \"process.env.VERCEL||(require('child_process').execSync('npx playwright install chromium',{stdio:'inherit'}))\""
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "archiver": "^7.0.1",
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.18.2",
    "pdf-lib": "^1.17.1",
    "@sparticuz/chromium": "^133.0.0",
    "playwright": "^1.40.0",
    "uuid": "^9.0.0"
  }
}
